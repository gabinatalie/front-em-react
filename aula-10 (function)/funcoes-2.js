// ! Funções Anônimas
//função que não tem nome

// não dei nome para a function, criei diretamente uma variável que recebe essa function.
const dobroDoNumero = function (numero) {
  return numero * 2;
};

const dobro = dobroDoNumero(2);
console.log(dobro);

// teoricamente, ambos os casos vai obter o resultado, anonima ou não, da na mesma.
// a diferença real está em casos mais complexos (funções de alta orgem)

